[project]
name = "mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sajil", email = "sajil.tariq@deltashoppe.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "mcp<2",
    "pydantic>=2.13.5",
    "starlette>=1.6.0",
    "uvicorn>=0.52.4",
]

[project.scripts]
mcp-demo = "mcp_demo:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
